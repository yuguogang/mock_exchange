{
    "name": "signal-translator",
    "version": "1.0.0",
    "description": "Translates replay-bot signals into mock-server orders",
    "main": "index.js",
    "scripts": {
        "test": "echo \"Error: no test specified\" && exit 1"
    },
    "dependencies": {
        "axios": "^1.13.3"
    }
}
